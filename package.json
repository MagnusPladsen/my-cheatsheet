{
  "name": "my-cheatsheet",
  "private": true,
  "version": "1.0.0",
  "workspaces": ["packages/*", "apps/*"],
  "scripts": {
    "dev:web": "bun run --filter @cheatsheet/web dev",
    "dev:desktop": "bun run --filter @cheatsheet/desktop tauri dev",
    "build:web": "bun run --filter @cheatsheet/web build",
    "build:desktop": "bun run --filter @cheatsheet/desktop tauri build"
  }
}
